# generated by terser; must stay byte-identical to the build output
bubz.min.js

# hand-formatted demo pages: a reformat here rewrites the whole file and hides
# real changes (and moving </pre> onto its own line adds a blank line to the
# rendered code block)
index.html
simpledemo.html

# The source is hand-formatted with JSDoc blocks and does not currently match
# Prettier's style, so a save would rewrite the whole file. If you would rather
# adopt Prettier here, delete this line and run it once as its own commit.
bubz.js
